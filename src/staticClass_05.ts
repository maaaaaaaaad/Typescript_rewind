//static is class level

class Person05 {
  static printPerson05(name: string, age: number): string {
    const result: string = `name : ${name}, age : ${age}`;
    return result;
  }
}

export default Person05;
